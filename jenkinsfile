pipeline {
    agent none

    stages {
        stage('Source') {
            steps {
                script {
                    echo 'Stage 1: Source'
                    echo "BUILD NUMBER = ${env.BUILD_NUMBER}"
             }
                
            }
        }
        stage('Test') {
            steps {
                echo 'Stage 2: Test'
                echo "BUILD NUMBER = ${env.BUILD_DISPLAY_NAME}"
            }
        }
        stage('Deploy') {
            steps {
                echo 'Stage 3: Deploy'
                echo "DATE = ${env.TAG_DATE}"
            }
        }
    }
}